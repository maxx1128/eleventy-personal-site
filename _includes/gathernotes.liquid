<script type="application/json" id="note-data">
  [
    {% for note in collections.notes %}
      {
        "title" : "{{ note.date | noteDate }}",
        "url"   : "{{ note.url }}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
</script>
