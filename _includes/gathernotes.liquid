<script type="application/json" id="note-data">
  [
    {% for note in collections.notes %}
      {
        "title" : "{{ note.data.date | date: metadata.note.dateFormat }}",
        "url"   : "{{ note.url }}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
</script>
