<script type="application/json" id="lesson-data">
  [
    {% for lesson in collections.todayILearned %}
      {
        "title" : "{{ lesson.data.title }}",
        "url"   : "{{ lesson.url }}"
      }{% unless forloop.last %},{% endunless %}
    {% endfor %}
  ]
</script>
